import React from 'react'

// TODO: real route instead of `get route`

const NotFoundPage = () => (<div>
    <h1>NOT FOUND</h1>
    <p>Sorry the route 'get route' does not exist.</p>
    <p>Do you?</p>
</div>)

export default NotFoundPage
